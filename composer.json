{
    "name": "mahbubhelal/fast-refresh-databases",
    "description": "A packge for making test faster by migrating databses only when the migrations are changed.",
    "type": "library",
    "version": "1.0",
    "keywords": ["laravel", "testing"],
    "license": "MIT",
    "authors": [
        {
            "name": "Mahbub Helal",
            "email": "mdhelal00@gmail.com"
        }
    ],
    "require": {
        "php": "^8.3"
    },
    "require-dev": {
        "orchestra/testbench": "^9.5",
        "pestphp/pest": "^3.5",
        "laravel/pint": "^1.18",
        "larastan/larastan": "^3.0",
        "rector/rector": "^2.0",
        "driftingly/rector-laravel": "^2.0",
        "phpstan/phpstan-strict-rules": "^2.0"
    },
    "autoload": {
        "psr-4": {
            "Mahbub\\FastRefreshDatabases\\": "src",
            "Database\\Factories\\Mahbub\\FastRefreshDatabases\\": "database/factories"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "Mahbub\\FastRefreshDatabases\\Tests\\": "tests"
        }
    },
    "extra": {
        "laravel": {
            "providers": [
                "Mahbub\\FastRefreshDatabases\\FastRefreshDatabasesServiceProvider"
            ]
        }
    },
    "config": {
        "allow-plugins": {
            "pestphp/pest-plugin": true
        }
    },
    "scripts": {
        "post-autoload-dump": [
            "@clear",
            "@prepare"
        ],
        "clear": "@php vendor/bin/testbench package:purge-skeleton --ansi",
        "prepare": "@php vendor/bin/testbench package:discover --ansi",
        "build": "@php vendor/bin/testbench workbench:build --ansi",
        "serve": [
            "Composer\\Config::disableProcessTimeout",
            "@build",
            "@php vendor/bin/testbench serve --ansi"
        ],
        "lint": [
            "@php vendor/bin/pint --ansi",
            "@php vendor/bin/phpstan analyse --verbose --ansi"
        ],
        "test": [
            "./vendor/bin/pint --test",
            "./vendor/bin/pest --coverage --min=100",
            "./vendor/bin/phpstan",
            "./vendor/bin/rector process --dry-run"
        ]
    }
}
